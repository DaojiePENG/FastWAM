# Follow-up work

- Migrate and evaluate the private action scheduler shift change from `5.0` to `0.5` in a separate PR; it is a training-recipe change and is intentionally excluded from the compile PR.
